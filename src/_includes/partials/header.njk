<nav>
  <a class="nav-logo" href="/"><span>Engage</span> Job Evaluation</a>
  <ul class="nav-links">
    {% for item in site.nav %}
    <li><a href="{{ item.url }}"{% if item.cta %} class="nav-cta"{% elif item.url | isActive(page.url) %} class="active"{% endif %}>{{ item.label }}</a></li>
    {% endfor %}
  </ul>
</nav>
